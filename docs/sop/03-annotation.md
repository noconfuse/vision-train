# SOP-3 数据标注

> 在项目中导入数据集，浏览图片，完成标注、复核和批量整理。

## 3.1 先把数据集放进项目

当前版本的数据集导入入口在“数据集列表”页，而不是项目设置里。

推荐步骤：

1. 先进入目标项目
2. 切到“数据集”视图
3. 点击“导入数据集”
4. 上传一个标准 YOLO 数据集 zip
5. 等待导入完成

导入成功后，数据集会出现在列表中，并可直接进入详情页。

## 3.2 合法的数据集结构

标准结构示例：

```text
<dataset_name>/
├── dataset.yaml
├── train/
│   ├── images/
│   └── labels/
├── val/
│   ├── images/
│   └── labels/
└── test/
    ├── images/
    └── labels/
```

其中：

- `dataset.yaml` 用来声明类别与 split
- `labels/` 里的每个标签文件与图片同名
- `test/` 可以没有，但后续测试评估会依赖它

## 3.3 数据集列表页可以做什么

在“数据集列表”里，你可以直接做这些操作：

- 打开数据集详情
- 进入训练页
- 重新分割 `train / val / test`
- 编辑数据集标签
- 下载整个数据集
- 删除数据集

如果你只是想开始标注，直接点击数据集行本身进入详情页即可。

## 3.4 数据集详情页的主要区域

进入数据集详情后，通常会看到：

- 顶部统计信息：图片数、标签数、标注率、类别概览
- 操作区：自动标注、生成子集、弱类补偿、合并、去重、标签整理等
- 过滤区：按 split、未标注、待复核筛选
- 图片网格：点击图片进入标注器

## 3.5 当前可用的筛选条件

当前版本常用筛选项如下：

| 筛选项 | 作用 |
| --- | --- |
| `split` | 在 `train`、`val`、`test` 之间切换 |
| 未标注 | 只看还没有人工标签的图片 |
| 待复核 | 只看存在自动标注草稿的图片 |
| 类别 chips | 只看包含指定类别的图片 |

每次调整后，点击“应用筛选”即可刷新结果。

## 3.6 单图标注怎么用

点击任意图片，会进入单图标注器。

当前标注器支持：

- 鼠标拖拽绘制新框
- 单击选中已有框
- 修改选中框类别
- 删除选中框
- 左右方向键切换上一张 / 下一张
- `Ctrl/Cmd + S` 主动保存当前图片
- `Delete` / `Backspace` 删除选中框
- `Esc` 关闭标注器

> 当前版本不要把“切图”或“关闭”理解为自动保存。需要落盘时，请主动使用 `Ctrl/Cmd + S` 或界面中的保存动作。

## 3.7 自动标注与待复核

如果这张图存在自动标注草稿，系统会把它标记为“待复核”。

你可以这样处理：

- 打开图片，核对自动框是否合理
- 删除误检
- 调整框位置或类别
- 补充漏标
- 确认无误后保存，转为正式标签

如果你想批量清空自动标注草稿，可以在数据集详情页使用“清除待复核标注”。

## 3.8 常见批量操作

### 批量删除图片

1. 打开选择模式
2. 勾选要删除的图片
3. 执行删除并确认

删除后，系统会同时清理对应图片与标签文件，操作不可恢复。

### 图片去重

当前版本的图片去重是按 **MD5** 识别重复文件，不是感知哈希。

适合处理：

- 完全重复的图片
- 多次导入后产生的同内容副本

不适合处理：

- 只是看起来相似、但像素并不完全一致的图片

### 合并数据集

可以把其他标准数据集并入当前数据集，用于统一整理训练素材。

### 标签顺序调整 / 删除标签

这两类操作都会直接改动 `dataset.yaml` 和标签文件，详见 [SOP-6 类别顺序调整](06-class-reorder.md)。

## 3.9 标签格式

每张图片的标签文件使用 YOLO 标准格式：

```text
<class_id> <x_center> <y_center> <width> <height>
```

说明：

- 坐标是归一化后的 `[0, 1]` 数值
- `class_id` 必须与 `dataset.yaml` 中的类别顺序一致
- 如果调整了类别顺序，必须同步重写历史标签

## 3.10 标注完成后怎么判断能不能训练

建议至少检查：

- `train` 集的关键类别是否已经基本标完
- 明显错标、漏标是否已经清理
- 未标注和待复核数量是否处于可接受范围

经验上：

- 如果数据量不大，尽量把训练集标完整再训练
- 如果数据量很大，也可以先完成核心样本，再先训练一版模型
- 第一版模型出来后，可以配合 [SOP-4 自动标注](04-auto-annotation.md) 回流复核

## 3.11 常见问题

- **为什么图片能看到，但不能直接训练？** 因为只有图片不够，训练需要足够完整的标签。
- **为什么筛“待复核”后还有图片？** 说明这些图片存在自动标注草稿，还没被人工确认。
- **为什么去重后删除数量比预期多？** 因为 MD5 去重会全量扫描并删除完全重复文件，不只针对这次新导入的数据。
- **为什么改了类别顺序后以前的标注看着错了？** 类别顺序改动会影响 `class_id` 语义，必须通过系统工具同步重写。
