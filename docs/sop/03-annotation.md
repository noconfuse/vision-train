# SOP-3 数据标注

> 把已规整好的 YOLO 数据集导入项目，然后给图片打 / 修正标签。

## 3.0 导入数据集

1. 选择一个**已存在的项目**（参考 [SOP-1 创建项目](01-project.md)）
2. 在该项目中**导入数据集**（zip 打包规范见 [SOP-1 §1.4](01-project.md#14-导入数据集zip)）
3. 等待上传完成 → 数据集落到 `projects/<project>/training/<name>/`
4. 项目数据集列表立即出现新数据集卡片

> 一个项目可挂多个数据集；互不冲突。

## 3.1 入口

进入某个数据集的**数据预览 / 标注视图**后，会看到：

- 该数据集的统计（图片数 / 标签数 / 标注率等）
- 类别 chips
- 图片网格（按 split / 类别 / 是否未标注过滤）

## 3.2 常用区域

| 区域                | 作用                                              |
| ------------------- | ------------------------------------------------- |
| 顶部统计行          | 图片/标签总数、标注率、类别 chips                 |
| 工具栏              | 全屏、选择模式、批量删除、调整标签顺序、删除标签、图片去重、合并数据集、弱类补偿 |
| 主区                | 图片网格（按 split / 类别 / 是否未标注过滤）     |
| 右侧（点击图片打开）| 单图标注面板                                     |

## 3.3 单图标注

### 打开标注器

点击任意图片缩略图，即可进入单图标注视图。

### 工具说明

| 操作                | 快捷键 / 鼠标        | 说明                                |
| ------------------- | -------------------- | ----------------------------------- |
| 绘制新框            | 鼠标拖拽             | 类别取自当前选中类别                |
| 选中已有框          | 单击                 | 边框变粗                            |
| 删除选中框          | `Delete` / `Backspace` |                                      |
| 切换类别            | 选中后从右侧类别列表点 |                                      |
| 上一张 / 下一张     | `←` / `→`           | 自动保存当前图                      |
| 全部保存并关闭      | 关闭按钮 / `Esc`     | 同步写回 `labels/<split>/`          |

### 类别切换

标注器右侧的类别列表，单击切换 **当前激活类别**。新建的 bbox 将使用该类别 ID。

> 类别 ID 必须与 `dataset.yaml::names` 中顺序一致，**不要在标注过程中临时改名**，否则后端校验会拒绝保存。

## 3.4 批量操作

### 批量删除

1. 启用选择模式 → 每张图片左上角出现复选框
2. 勾选要删除的图片（也可 **全选本页**）
3. 触发删除 → 二次确认 → 后端同时删除 `images/<file>` 与 `labels/<file>.txt`

> 删除是**不可恢复**的，系统只做二次确认校验，不会移到回收站。

### 类别顺序调整

参见 [SOP-6 类别顺序调整](06-class-reorder.md)。

### 删除整个类别

参见 [SOP-6 类别顺序调整](06-class-reorder.md) 末节。

### 图片去重

1. 触发**图片去重**功能 → 后端按 pHash 扫描
2. 弹出列表显示重复组，勾选要删除的副本
3. 确认后删除

> 去重**只比较图片**（不解码像素差异），速度比感知哈希快很多；如需严格去重请自行预处理。

## 3.5 过滤与翻页

主区上方过滤条支持的过滤项：

- **split**：`train` / `val` / `test` / `all`
- **类别筛选**：多选，仅显示包含选中类别之一的图片
- **仅未标注**：勾选后只显示还没有 `.txt` 的图片
- **Person 复核**（可选）：对人物类特殊高亮

分页：每页 200 张（可在 UI 中改）。

## 3.6 标签格式

每张图的标签是同名 `.txt`：

```text
<class_id> <x_center> <y_center> <width> <height>
```

坐标是**归一化**到 `[0, 1]`，YOLO 标准。

> 系统**不会**校验坐标是否超出图片边界，**也不会**自动修复越界框——保证不会静默改你的数据。

## 3.7 标注完成度检查

- 看顶部 **标注率**：`labels / images`。
- 一般而言 `train` ≥ 95% 时即可进入 [SOP-7 训练](07-train.md)
- 如果数据量大，标注到 60%~80% 可以先做一版训练 + 评估，再用 [SOP-4 自动标注](04-auto-annotation.md) 把模型回流到标注流程
